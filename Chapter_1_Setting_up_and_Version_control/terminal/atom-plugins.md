# Give Atom some super power 😎🚀

All the following atom packages can be installed from your terminal. Just copy every line without the `$`

### Javascript

```
$ apm install atom-ternjs
$ apm install atom-typescript
```

### Testing

```
$ apm install cucumber
$ apm install cucumber-autocomplete
$ apm install cucumber-step
$ apm install gherkin-autocomplete

$ apm install rails-rspec
$ apm install rspec
```

### UI Tweaks

```
$ apm install atom-material-syntax
$ apm install atom-material-ui
$ apm install file-icons
```

### Git

```
$ apm install git-control
$ apm install git-diff
$ apm install git-history
$ apm install git-plus
$ apm install merge-conflicts

$ apm install go-to-line
$ apm install goto-definition
```

### Language Syntax highlighting

```
$ apm install language-coffee-script
$ apm install language-css
$ apm install language-diff
$ apm install language-gherkin
$ apm install language-git
$ apm install language-haml
$ apm install language-html
$ apm install language-hyperlink
$ apm install language-javascript
$ apm install language-json
$ apm install language-less
$ apm install language-rspec
$ apm install language-ruby
$ apm install language-ruby-on-rails
$ apm install language-sass
$ apm install language-slim
$ apm install language-yaml
$ apm install line-ending-selector
```

### Linters

```
$ apm install linter
$ apm install linter-coffeelint
$ apm install linter-haml
$ apm install linter-hlint
$ apm install linter-htmlhint
$ apm install linter-js-yaml
$ apm install linter-jshint
$ apm install linter-jsonlint
$ apm install linter-less
$ apm install linter-rubocop
$ apm install linter-scss-lint
$ apm install linter-slim
$ apm install linter-write-good
```

### Utilities

```
$ apm install aligner-scss
$ apm install atom-beautify
$ apm install atom-html-preview
$ apm install auto-indent
$ apm install autoclose-html
$ apm install autocomplete-atom-api
$ apm install autocomplete-css
$ apm install autocomplete-html
$ apm install autocomplete-paths
$ apm install autocomplete-plus
$ apm install autocomplete-snippets
$ apm install autoflow
$ apm install autosave
$ apm install bracket-matcher
$ apm install color-picker
$ apm install double-tag
$ apm install emmet
$ apm install markdown-pdf
$ apm install markdown-preview
$ apm install minimap
$ apm install multi-cursor
$ apm install pigments
$ apm install pretty-json
$ apm install sort-lines
$ apm install spell-check
$ apm install status-bar
$ apm install symbols-view
$ apm install sync-settings
$ apm install tabs
$ apm install tool-bar
$ apm install tool-bar-main
$ apm install trailing-spaces
$ apm install whitespace
```

### Dependencies for some packages

```
$ gem install scss-lint
$ gem install rubocop
$ gem install coffee-script
$ npm install -g coffeelint
```



